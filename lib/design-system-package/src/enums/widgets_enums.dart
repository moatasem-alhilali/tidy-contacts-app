part of 'enums.dart';

enum AxisAlignment {
  start,
  end,
  center,
  spaceBetween,
  spaceAround,
  spaceEvenly,
  baseline,
  stretch;
}

enum WidgetAlignment {
  center,
  centerStart,
  centerEnd,
  topCenter,
  topStart,
  topEnd,
  bottomCenter,
  bottomStart,
  bottomEnd;
}

enum TextWidgetAlign {
  start,
  end,
  center,
  left,
  right,
  justify;
}

enum AxisSize {
  min,
  max;
}
