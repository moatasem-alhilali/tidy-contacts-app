part of '../app_theme.dart';

@immutable
class _TabletCorners implements BaseCorners {
  @override
  final Radius rs = Radius.circular(8.r);
  @override
  final Radius rm = Radius.circular(12.r);
  @override
  final Radius rl = Radius.circular(15.r);
  @override
  final Radius rbi = Radius.circular(32.r);
  @override
  final Radius rb = Radius.circular(12.r);
  @override
  final Radius rc = Radius.circular(15.r);
  @override
  final Radius rc360 = Radius.circular(360.r);
}
