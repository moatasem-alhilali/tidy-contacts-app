part of '../app_theme.dart';

@immutable
class _DesktopCorners implements BaseCorners {
  @override
  final Radius rs = const Radius.circular(8);
  @override
  final Radius rm = const Radius.circular(12);
  @override
  final Radius rl = const Radius.circular(15);
  @override
  final Radius rbi = const Radius.circular(32);
  @override
  final Radius rb = const Radius.circular(12);
  @override
  final Radius rc = const Radius.circular(15);
  @override
  final Radius rc360 = const Radius.circular(360);
}
